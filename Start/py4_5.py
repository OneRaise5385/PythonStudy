# 元组
# 不可变的列表称为元组
dimensions = (200,50)
print(dimensions[0])

# 遍历元组
for dimension in dimensions:
    print(dimension)

# 修改元组只能修改整个元组，用赋值的方法
dimensions = [200,400]
print(dimensions)

# 代码规范
# 缩进时不能将空格键和制表符混用
# 每行控制代码的字符数不超过80